import db from './db.json';

export const projectsCards = db.projectsCards;
export const interest = db.interest;
export const timelineItems = db.timelineItems;
export const certificates = db.certificates;
